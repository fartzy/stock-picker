load("@rules_python//python:defs.bzl", "py_library")

py_library(
    name = "tickers",
    srcs = [
        "manual_additions.py",
        "universe.py",
    ],
    imports = ["../.."],
    visibility = ["//visibility:public"],
    deps = [
        "@pypi//lxml",  # keep: implicit pandas.read_html backend, no direct import
        "@pypi//pandas",
        "@pypi//requests",
    ],
)
