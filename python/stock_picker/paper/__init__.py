"""Paper book: morning-list names held Open->Close.

Not model features and not the Fidelity log. Persistence is PaperBookStore
(SQLite). This package rebuilds from ScanStore + PriceStore and slices
Fit vs Rank / top-K for the What if tab.
"""
