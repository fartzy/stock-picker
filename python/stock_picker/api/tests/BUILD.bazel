# gazelle:python_generation_mode file
load("//bazel/rules/pytest:defs.bzl", "py_pytest")

py_pytest(
    name = "test_routes",
    srcs = ["test_routes.py"],
    imports = ["../../.."],
    deps = [
        "//python/stock_picker/api",
        "//python/stock_picker/features/tests:fixtures",
        "//python/stock_picker/storage",
        "@pypi//fastapi",
        "@pypi//httpx",  # keep: FastAPI's TestClient needs httpx as its transport, no direct import
        "@pypi//pandas",
    ],
)
